<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Required for Electron on a hardened runtime: V8 JITs JS into
         writable executable pages. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- electron-vite ships native modules like audify via dlopen; the
         loader must be able to resolve non-bundled .dylibs/.node files. -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- LTC decoding = raw audio capture. Without this entitlement + the
         usage-description Info.plist string, macOS silently blocks the
         stream. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- dLive bridge = outbound TCP to 192.168.x.x. -->
    <key>com.apple.security.network.client</key>
    <true/>
  </dict>
</plist>
